# Timba Predictor

App de predicción de partidos (Streamlit) y utilidades CLI.

Instalación rápida:

```bash
python -m pip install -r requirements.txt
```

Ejecutar la app web:

```bash
streamlit run app.py --server.port 8502
```

Ejecutar CLI:

```bash
python cli.py
```
